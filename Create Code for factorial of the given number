#include <iostream> 
using namespace std; 
 
unsigned int fact(unsigned int n) 
{ 
    if (n == 0) 
        return 1; 
    return n * fact(n - 1); 
} 
  

int main() 
{ 
    int n;
    
    cin>>n;
    
    cout << "Factorial of "<< n<< " is " << fact(n) << endl; 
    return 0; 
} 
